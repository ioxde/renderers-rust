//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::GovernanceAccountType;
use crate::generated::types::RealmConfig;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;
use spl_collections::TrailingStr;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RealmV1 {
    pub account_type: GovernanceAccountType,
    pub community_mint: Address,
    pub config: RealmConfig,
    pub reserved: [u8; 6],
    pub voting_proposal_count: u16,
    pub authority: Option<Address>,
    pub name: String,
}

impl RealmV1 {
    /// Prefix values used to generate a PDA for this account.
    ///
    /// Values are positional and appear in the following order:
    ///
    ///   0. `RealmV1::PREFIX`
    ///   1. name (`TrailingStr`)
    pub const PREFIX: &'static [u8] = b"governance";

    pub fn create_pda(
        name: TrailingStr,
        bump: u8,
    ) -> Result<solana_address::Address, solana_address::error::AddressError> {
        solana_address::Address::create_program_address(
            &[b"governance", name.to_string().as_ref(), &[bump]],
            &crate::SPL_GOVERNANCE_ID,
        )
    }

    pub fn find_pda(name: TrailingStr) -> (solana_address::Address, u8) {
        solana_address::Address::find_program_address(
            &[b"governance", name.to_string().as_ref()],
            &crate::SPL_GOVERNANCE_ID,
        )
    }

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for RealmV1 {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        if account_info.owner != &crate::SPL_GOVERNANCE_ID {
            return Err(std::io::Error::new(
                std::io::ErrorKind::InvalidData,
                "invalid account owner",
            ));
        }
        let data: &[u8] = &(*account_info.data).borrow();
        Self::from_bytes(data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_realm_v1(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<RealmV1>, std::io::Error> {
    let accounts = fetch_all_realm_v1(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_realm_v1(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<RealmV1>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<RealmV1>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        if account.owner != crate::SPL_GOVERNANCE_ID {
            return Err(std::io::Error::other(format!(
                "Invalid owner for account: {address}"
            )));
        }
        let data = RealmV1::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_realm_v1(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<RealmV1>, std::io::Error> {
    let accounts = fetch_all_maybe_realm_v1(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_realm_v1(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<RealmV1>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<RealmV1>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            if account.owner != crate::SPL_GOVERNANCE_ID {
                return Err(std::io::Error::other(format!(
                    "Invalid owner for account: {address}"
                )));
            }
            let data = RealmV1::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for RealmV1 {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for RealmV1 {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for RealmV1 {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::SPL_GOVERNANCE_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for RealmV1 {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for RealmV1 {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
