//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::SetRealmAuthorityAction;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_REALM_AUTHORITY_DISCRIMINATOR: u8 = 21;

/// Accounts.
#[derive(Debug)]
pub struct SetRealmAuthority {
    pub realm_account: solana_pubkey::Pubkey,

    pub realm_authority: solana_pubkey::Pubkey,
    /// Must be one of the realm governances when set
    pub new_realm_authority: Option<solana_pubkey::Pubkey>,
}

impl SetRealmAuthority {
    pub fn instruction(
        &self,
        args: SetRealmAuthorityInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetRealmAuthorityInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.realm_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.realm_authority,
            true,
        ));
        if let Some(new_realm_authority) = self.new_realm_authority {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                new_realm_authority,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SPL_GOVERNANCE_ID,
                false,
            ));
        }
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetRealmAuthorityInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SPL_GOVERNANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetRealmAuthorityInstructionData {
    discriminator: u8,
}

impl SetRealmAuthorityInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 21 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetRealmAuthorityInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetRealmAuthorityInstructionArgs {
    pub action: SetRealmAuthorityAction,
}

impl SetRealmAuthorityInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetRealmAuthority`.
///
/// ### Accounts:
///
///   0. `[writable]` realm_account
///   1. `[signer]` realm_authority
///   2. `[optional]` new_realm_authority
#[derive(Clone, Debug, Default)]
pub struct SetRealmAuthorityBuilder {
    realm_account: Option<solana_pubkey::Pubkey>,
    realm_authority: Option<solana_pubkey::Pubkey>,
    new_realm_authority: Option<solana_pubkey::Pubkey>,
    action: Option<SetRealmAuthorityAction>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetRealmAuthorityBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn realm_account(&mut self, realm_account: solana_pubkey::Pubkey) -> &mut Self {
        self.realm_account = Some(realm_account);
        self
    }
    #[inline(always)]
    pub fn realm_authority(&mut self, realm_authority: solana_pubkey::Pubkey) -> &mut Self {
        self.realm_authority = Some(realm_authority);
        self
    }
    /// `[optional account]`
    /// Must be one of the realm governances when set
    #[inline(always)]
    pub fn new_realm_authority(
        &mut self,
        new_realm_authority: Option<solana_pubkey::Pubkey>,
    ) -> &mut Self {
        self.new_realm_authority = new_realm_authority;
        self
    }
    #[inline(always)]
    pub fn action(&mut self, action: SetRealmAuthorityAction) -> &mut Self {
        self.action = Some(action);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetRealmAuthority {
            realm_account: self.realm_account.expect("realm_account is not set"),
            realm_authority: self.realm_authority.expect("realm_authority is not set"),
            new_realm_authority: self.new_realm_authority,
        };
        let args = SetRealmAuthorityInstructionArgs {
            action: self.action.clone().expect("action is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_realm_authority` CPI accounts.
pub struct SetRealmAuthorityCpiAccounts<'a, 'b> {
    pub realm_account: &'b solana_account_info::AccountInfo<'a>,

    pub realm_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Must be one of the realm governances when set
    pub new_realm_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
}

/// `set_realm_authority` CPI instruction.
pub struct SetRealmAuthorityCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub realm_account: &'b solana_account_info::AccountInfo<'a>,

    pub realm_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Must be one of the realm governances when set
    pub new_realm_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// The arguments for the instruction.
    pub __args: SetRealmAuthorityInstructionArgs,
}

impl<'a, 'b> SetRealmAuthorityCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetRealmAuthorityCpiAccounts<'a, 'b>,
        args: SetRealmAuthorityInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            realm_account: accounts.realm_account,
            realm_authority: accounts.realm_authority,
            new_realm_authority: accounts.new_realm_authority,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.realm_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.realm_authority.key,
            true,
        ));
        if let Some(new_realm_authority) = self.new_realm_authority {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *new_realm_authority.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SPL_GOVERNANCE_ID,
                false,
            ));
        }
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SetRealmAuthorityInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SPL_GOVERNANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.realm_account.clone());
        account_infos.push(self.realm_authority.clone());
        if let Some(new_realm_authority) = self.new_realm_authority {
            account_infos.push(new_realm_authority.clone());
        }
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetRealmAuthority` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` realm_account
///   1. `[signer]` realm_authority
///   2. `[optional]` new_realm_authority
#[derive(Clone, Debug)]
pub struct SetRealmAuthorityCpiBuilder<'a, 'b> {
    instruction: Box<SetRealmAuthorityCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetRealmAuthorityCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetRealmAuthorityCpiBuilderInstruction {
            __program: program,
            realm_account: None,
            realm_authority: None,
            new_realm_authority: None,
            action: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn realm_account(
        &mut self,
        realm_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.realm_account = Some(realm_account);
        self
    }
    #[inline(always)]
    pub fn realm_authority(
        &mut self,
        realm_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.realm_authority = Some(realm_authority);
        self
    }
    /// `[optional account]`
    /// Must be one of the realm governances when set
    #[inline(always)]
    pub fn new_realm_authority(
        &mut self,
        new_realm_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.new_realm_authority = new_realm_authority;
        self
    }
    #[inline(always)]
    pub fn action(&mut self, action: SetRealmAuthorityAction) -> &mut Self {
        self.instruction.action = Some(action);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetRealmAuthorityInstructionArgs {
            action: self.instruction.action.clone().expect("action is not set"),
        };
        let instruction = SetRealmAuthorityCpi {
            __program: self.instruction.__program,

            realm_account: self
                .instruction
                .realm_account
                .expect("realm_account is not set"),

            realm_authority: self
                .instruction
                .realm_authority
                .expect("realm_authority is not set"),

            new_realm_authority: self.instruction.new_realm_authority,
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetRealmAuthorityCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    realm_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    realm_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_realm_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    action: Option<SetRealmAuthorityAction>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
