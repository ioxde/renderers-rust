//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::events::TradeEvent;
use crate::generated::events::TRADE_EVENT_DISCRIMINATOR;
use borsh::BorshDeserialize;

/// Event kinds for the `alpha` program.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum AlphaEventKind {
    TradeEvent,
}

/// Identifies a `alpha` event from the provided data.
///
/// A foreign `program_id` is a `None`, not an error: callers iterate whole transactions where
/// other programs are ordinary.
pub fn identify_alpha_event(
    program_id: &solana_address::Address,
    data: &[u8],
) -> Option<AlphaEventKind> {
    if program_id != &crate::ALPHA_ID {
        return None;
    }
    if data.get(..TRADE_EVENT_DISCRIMINATOR.len()) == Some(&TRADE_EVENT_DISCRIMINATOR[..]) {
        return Some(AlphaEventKind::TradeEvent);
    }
    None
}

/// Parsed event variants for the `alpha` program.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum AlphaEvent {
    TradeEvent(TradeEvent),
}

/// Tries to parse a `alpha` event from the provided data.
///
/// `None` when [`identify_alpha_event`] finds nothing; `Some(Err(_))`
/// when one matches but fails to deserialize. Use [`Option::transpose`] to propagate failures
/// with `?`, or scan a batch:
///
/// ```ignore
/// let events: Vec<AlphaEvent> = datas
///     .iter()
///     .filter_map(|data| try_parse_alpha_event(program_id, data))
///     .collect::<Result<_, _>>()?;
/// ```
pub fn try_parse_alpha_event(
    program_id: &solana_address::Address,
    data: &[u8],
) -> Option<Result<AlphaEvent, std::io::Error>> {
    let event_kind = identify_alpha_event(program_id, data)?;
    Some(match event_kind {
        AlphaEventKind::TradeEvent => {
            let mut data = &data[8..];
            TradeEvent::deserialize(&mut data).map(AlphaEvent::TradeEvent)
        }
    })
}
