//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::events::BetaTradeEvent;
use crate::generated::events::BETA_TRADE_EVENT_DISCRIMINATOR;
use borsh::BorshDeserialize;

/// Event kinds for the `beta` program.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BetaEventKind {
    BetaTradeEvent,
}

/// Identifies a `beta` event from the provided data.
///
/// A foreign `program_id` is a `None`, not an error: callers iterate whole transactions where
/// other programs are ordinary.
pub fn identify_beta_event(
    program_id: &solana_address::Address,
    data: &[u8],
) -> Option<BetaEventKind> {
    if program_id != &crate::BETA_ID {
        return None;
    }
    if data.get(..BETA_TRADE_EVENT_DISCRIMINATOR.len()) == Some(&BETA_TRADE_EVENT_DISCRIMINATOR[..])
    {
        return Some(BetaEventKind::BetaTradeEvent);
    }
    None
}

/// Parsed event variants for the `beta` program.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BetaEvent {
    BetaTradeEvent(BetaTradeEvent),
}

/// Tries to parse a `beta` event from the provided data.
///
/// `None` when [`identify_beta_event`] finds nothing; `Some(Err(_))`
/// when one matches but fails to deserialize. Use [`Option::transpose`] to propagate failures
/// with `?`, or scan a batch:
///
/// ```ignore
/// let events: Vec<BetaEvent> = datas
///     .iter()
///     .filter_map(|data| try_parse_beta_event(program_id, data))
///     .collect::<Result<_, _>>()?;
/// ```
pub fn try_parse_beta_event(
    program_id: &solana_address::Address,
    data: &[u8],
) -> Option<Result<BetaEvent, std::io::Error>> {
    let event_kind = identify_beta_event(program_id, data)?;
    Some(match event_kind {
        BetaEventKind::BetaTradeEvent => {
            let mut data = &data[8..];
            BetaTradeEvent::deserialize(&mut data).map(BetaEvent::BetaTradeEvent)
        }
    })
}
